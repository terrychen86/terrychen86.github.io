language: node_js
node_js:
  - "12.8.0"
cache:
  directories:
  - node_modules
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  local_dir: public
  keep-history: true
  on:
    branch: master
before_script:
  - "npm install -g gatsby-cli"
  - "npm install"
script:
  - "npm run test"
after_success:
  - "npm run deploy"
